const BASE_URL = "http://localhost:3000";
const API = "api";
const API_VERSION = "v1";
const DATABASE = "database";

const BASE_ROUTE = `${BASE_URL}/${API}/${API_VERSION}/${DATABASE}`;

export const USERS_ROTE = `${BASE_ROUTE}/users`;
